# ToDoアプリ 依存パッケージ
# tkinter は Python 標準ライブラリに含まれるため pip install 不要
# 以下は追加で必要なパッケージ（現時点では標準ライブラリのみで実装可能）

# 標準ライブラリのみ使用のため、追加インストール不要
# - tkinter  (GUI)
# - json     (データ保存)
# - datetime (日時管理)
# - os       (ファイル操作)
# - uuid / int (ID管理)
